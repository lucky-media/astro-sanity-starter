import type { Alpine } from "alpinejs";

export default (Alpine: Alpine) => {
  // Register Alpine plugins here
  // Example: Alpine.plugin(collapse);
  // Register Alpine components here
  // Example: Alpine.data("myComponent", () => ({ ... }));
};
