{
  "name": "astro-sanity-starter",
  "version": "0.0.1",
  "private": true,
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typegen": "turbo run typegen --filter=studio"
  },
  "devDependencies": {
    "turbo": "^2.6.3"
  }
}
